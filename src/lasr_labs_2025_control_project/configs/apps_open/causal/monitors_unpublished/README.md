Configs whose monitor-output eval logs are not part of the published dataset
(hf.co/datasets/jbostock/untrusted-monitoring-2026-paper). Kept out of the
`monitors/` directory so `plot_everything.sh` runs cleanly out of the box;
move them back if the corresponding outputs are ever uploaded.
